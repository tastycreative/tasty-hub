import { TeamMembersView } from "@/features/team/components";

/**
 * Team Members Page
 * This page is shared across all team types (Admin, HR, Team)
 * Uses the TeamMembersView component from features/team/components
 */
export default function TeamMembersPage() {
  return <TeamMembersView />;
}
